{
  "name": "unolo-tracker-backend",
  "version": "1.0.0",
  "description": "Field Force Tracker API",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "nodemon server.js",
    "init-db": "node scripts/init-db.js",
    "setup": "npm install && npm run init-db",
    "test": "vitest run",
    "test:watch": "vitest",
    "test:coverage": "vitest run --coverage"
  },
  "dependencies": {
    "bcryptjs": "^2.4.3",
    "better-sqlite3": "^12.6.2",
    "cors": "^2.8.5",
    "dotenv": "^16.6.1",
    "express": "^4.18.2",
    "jsonwebtoken": "^9.0.2"
  },
  "devDependencies": {
    "@vitest/coverage-v8": "^1.1.0",
    "nodemon": "^3.0.2",
    "supertest": "^6.3.3",
    "vitest": "^1.1.0"
  }
}
